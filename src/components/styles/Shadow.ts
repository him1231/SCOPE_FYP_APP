import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  shadow: {
    shadowColor: '#939393',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.23,
    shadowRadius: 5,

    elevation: 3,
  },
});

export default styles.shadow;
